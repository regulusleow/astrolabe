interface:
  display_name: "Astrolabe UI Inspector"
  short_description: "Verify a running mobile app UI against design contracts and Runtime evidence"
  default_prompt: "Use Astrolabe MCP to observe the real target state, prepare it only through authorized visible UI or developer assistance, capture a fresh acceptance snapshot, and verify design contracts, rendered content, styles, UI relations, and screenshots with capability-driven evidence."
